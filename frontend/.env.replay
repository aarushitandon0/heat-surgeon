# `npm run build:replay`: a static build that never calls a backend and replays the recorded snapshot
# (frontend/public/snapshot/, from `python -m app.snapshot`). The header says it is a recording on every stage.
VITE_REPLAY_ONLY=true
